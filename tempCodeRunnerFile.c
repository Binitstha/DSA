
    // {
    //     printf("[%p | %d | %p]\n", temp, temp->data, temp->next);
    //     temp = temp->next;
    // } while (temp->next != head);
    // printf("[%p | %d | %p]\n", temp, temp->data, temp->next);
    // last = temp;